<h5 class='offset-md-2'>ログイン</h5>
<div class='container'>
  <h6 class='offset-md-2 pt-1'>会員の方はこちらからログイン</h6>
  <div class='row'>
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 pt-1 mx-auto">
      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <div class="field">
          <%= f.label :email, "メールアドレス", class: "col-md-4" %>
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "col-md-6" %>
        </div>

        <div class="field">
          <%= f.label :password, "パスワード", class: "col-md-4" %>
          <%= f.password_field :password, autocomplete: "current-password", class: "col-md-6" %>
        </div>

        <div class="actions mt-2">
          <%= f.submit "ログイン", class: 'btn btn-sm btn-primary px-4 offset-md-4' %>
        </div>
      <% end %>
    </div>
  </div>
  <h6 class='offset-md-2 pt-5'>会員登録がお済みでない方</h6>
  <div class='row'>
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 pt-1 mx-auto">
      <p><%= link_to "こちら", new_registration_path(resource_name) %>　から新規登録を⾏ってください。</p>
    </div>
  </div>
</div>
