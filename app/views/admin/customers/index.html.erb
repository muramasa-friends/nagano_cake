<div class="container">
  <h2>会員一覧</h2>
  <table class="table">
    <thead>
      <tr>
        <th>会員ID</th>
        <th>氏名</th>
        <th>メールアドレス</th>
        <th>ステータス</th>
      </tr>
    </thead>
    <tbody>
      <% @admin_customers.each do |admin_customer| %>
        <tr>
          <td><%= admin_customer.id%></td>
          <td><%= admin_customer.last_name %><%= admin_customer.first_name %></td>
          <td><%= admin_customer.email %></td>
          
          <td>
            <% if admin_customer.is_deleted %>
              <%= "退会" %>
            <% else %>
              <%= "有効" %>
            <% end %>
          </td>
          
        </tr>
      <% end %>
    </tbody>
  </table>
  
  <div class="col-md-1 col-md-offset-6 mx-auto">
    <%= paginate @admin_customers %>
  </div>
  
</div>
